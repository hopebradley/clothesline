import React from 'react';

const Cart = (props) => {
    return (
        <div className="cart">
            <h1>I am the cart</h1>
            {/* {props.cartContents.map(item => {
                return "Theres a new Cart item now";
            })} */}
        </div>
    )
}

export default Cart;